{
  "name": "angularnode",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build" : "",
    "watch" : "concurrently \"npm run watch-server\" \"npm run watch-client\"",
    "watch-server": "cd client && npm start",
    "watch-client": "cd server && npm run watch",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^4.0.1"
  }
}
